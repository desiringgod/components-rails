source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in components-rails.gemspec
gemspec

group :test, :development do
  gem 'guides', github: 'desiringgod/guides'
  gem 'rubocop'
  gem 'rubocop-rspec'
end
